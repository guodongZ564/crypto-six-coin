# Claude Code 规格 1 / 3 · 采集 + 历史回填 + 异动报警

> 三份规格按 1→2→3 顺序贴给 Claude Code。本份取代之前的「阶段一」文档（把一次性历史回填并了进来）。
> 贴给 Claude Code 后先让它复述对目标和约束的理解，确认无误再动手。凭据用占位符，实际值放 GitHub Secrets。

---

## 目标

为「即时策略站」搭一个自动化因子监控系统，覆盖 **BTC / ETH / SOL / UNI / LINK / LTC** 加宏观、行业周期因子。本份做三件事：

1. **历史回填（一次性）**：把能拿到多年历史的因子灌进时间序列库，让系统启动即满状态。
2. **每日采集**：每天抓当日值，增量追加。
3. **异动报警**：滚动 z-score，`|z|>阈值` 推 Telegram。

**核心原则**：所有信号由确定性代码算，本份不接 LLM；严格防未来函数（z-score 只用截至前一日的窗口）；单源失败不中断整轮；因子清单/阈值写在 `config/factors.yaml`。

---

## 技术约束

- Python 3.11；调度 GitHub Actions，每日北京时间 08:30（UTC cron `30 0 * * *`）
- 依赖：`ccxt pandas numpy requests pyyaml pyarrow`（Telegram 直接用 requests 调 Bot API）
- 存储：仓库内 `data/factor_timeseries.parquet`，长格式，回填后每日增量追加并 git commit & push
- Secrets：`TELEGRAM_BOT_TOKEN` `TELEGRAM_CHAT_ID` `FRED_API_KEY` `CRYPTOQUANT_API_KEY`

## 落盘表结构（长格式）

`date | asset | factor | value | source`
- asset ∈ {BTC,ETH,SOL,UNI,LINK,LTC,MACRO,MARKET}
- 追加前按 (date,asset,factor) 去重覆盖

---

## 一次性回填 `backfill.py`

按下表各源拉尽可能长的历史，写进同一张 parquet。跑一次即可，之后靠每日采集增量。

| 因子组 | 源 | 可回填深度 | 备注 |
|---|---|---|---|
| OHLCV 日/周/月线 → MA50/200、200周线、月线MACD、RSI、MACD、布林带、量比、相对强度 | ccxt/Binance | 多年 | 骨架，全部可算 |
| 永续资金费率 | Binance `/fapi/v1/fundingRate` | 永续上线至今 | 分页拉全量 |
| 恐慌贪婪指数 | Alternative.me `fng/?limit=0` | 2018 至今 | 一次全量 |
| 宏观：核心CPI/核心PCE/M2/联邦基金/10Y | FRED（series id 待填） | 数十年 | 见下方 vintage 提醒 |
| DVOL 波动率 | Deribit 历史端点 | 多年 | BTC/ETH |
| 各链 TVL / 稳定币 / DEX量 / 协议费用 | DefiLlama `/charts` 系列 | 多年 | ETH/SOL链、Uniswap |
| 交易所净流/盈亏供应比/利润供应% | CryptoQuant Pro 历史端点 | 多年 | 有 key 才跑 |
| Coinbase 溢价 | Coinbase vs Binance 历史价自算 | 多年 | 代码计算 |
| 未平仓 OI、大户多空比 | Binance 公共统计 | **仅约30天** | 历史薄，从现在起自记增量 |
| 治理/机构/KOL 等定性项 | — | 无 | 不回填、不量化 |

**FRED vintage 提醒（只影响后续回测，不影响线上）**：FRED 默认给的是修正后终值。宏观因子若要用于回测，需从 ALFRED 取「初次发布值」以防偷看未来；本份先把终值落库供线上用，并在 factor 上打标 `revised=true`，供规格3 处理。DefiLlama/CryptoQuant 指标口径历史上改过定义，回填时记录数据起始日，跨口径断点在规格3 里规避。

---

## 每日采集 `run_daily.py`

流程：采集当日各源 → 去重追加 parquet → 异动检测 → Telegram 推送 → 打印运行摘要（采集因子数、异动数、失败源数）→ git commit push。每个 collector 包 try/except，失败记摘要不中断。

## 异动检测 `core/anomaly.py`

对每个 (asset,factor)：取截至**前一日**最近 N 天窗口（config，默认90，<30 标「历史不足」跳过）→ `z=(今值-μ)/σ` → `|z|>阈值`（默认2.0）报警。另加硬阈值开关：资金费率极端、恐慌贪婪 <20/>80 即使 z 未超也报。方向 z>0 记▲、z<0 记▼，多空含义文案从 config 读（先留占位，规格2 用规则表补全）。

## Telegram 报警格式

```
📡 因子异动 · 2026-07-20 08:30
🔴 SOL 资金费率 ▲ z=+2.8  当前0.061%/8h（90日均0.012%）→ 多头过热
🟢 BTC 恐慌贪婪 ▼ 值=18  极度恐慌 → 逆向偏多
—— 采集42因子 异动2 失败源0
```
无异动也发一条极简确认「✅ 今日无异动 采集N 失败M」。

---

## 仓库结构

```
.github/workflows/daily.yml
config/factors.yaml
collectors/  ccxt_market.py defillama.py fred.py cryptoquant.py deribit.py sentiment.py coinbase_premium.py
core/  store.py anomaly.py alert.py
backfill.py        # 一次性
run_daily.py       # 每日入口
requirements.txt
```

## 验收标准

1. `python backfill.py` 跑完后 parquet 里 OHLCV类/资金费率/恐慌贪婪/宏观/DefiLlama 因子有多年历史。
2. `python run_daily.py` 无 CryptoQuant key 也能跑完（自动跳过），增量追加成功。
3. 配好 Telegram secrets 能真实收到消息。
4. 任一单源报错，整轮不崩，摘要正确标注失败源。
5. z-score 只用前一日及更早窗口（防未来函数）。
6. GitHub Actions 手动触发成功并提交当日 parquet。

## 本份不做

不评分、不出买卖信号、不接 LLM、不做回测；不接 ETF流/Coinglass付费/爬虫源（后补）。
